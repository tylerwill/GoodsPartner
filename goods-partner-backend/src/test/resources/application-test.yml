spring:
  flyway:
    enabled: true
    locations: classpath:db/migration, db/test
  jpa:
    properties:
      hibernate:
        format_sql: false
        show_sql: false

google:
  api:
    key: some-test-key

reports:
  destination: reports
