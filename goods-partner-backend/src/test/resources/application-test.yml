spring:
  datasource:
    url: jdbc:postgresql://localhost/gp-local
    username: postgres
    password: root
  flyway:
    enabled: false
    locations: classpath:db/migration, db/test
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true

google:
  api:
    key: some-test_key

reports:
  destination: reports
