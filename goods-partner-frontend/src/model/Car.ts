// TODO: Replace user string with User

export interface Car {
    available: boolean,

    cooler: boolean,
    driver: string,
    id: number,
    licencePlate: string,
    loadSize: number,
    name: string,
    travelCost: number,
    weightCapacity: number
}